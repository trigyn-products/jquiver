# Jquiver Extension

This extension generates templates and project scaffolding using API integration.

## Features
- Loads templates from API
- Generates files in VS Code workspace
- Supports project scaffolding

## Usage
Open VS Code → Open project → extension activates automatically.
